@page
@model ResourceAssignAdmin.Pages.Plan.IndexModel

@{
    ViewData["Title"] = "Plan";
}

<h1>Plan</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlanSubscription[0].Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlanSubscription[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlanSubscription[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlanSubscription[0].Duration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlanSubscription[0].CreateDatetime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.PlanSubscription)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Duration)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreateDatetime)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
